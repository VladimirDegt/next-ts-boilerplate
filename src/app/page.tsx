import cls from './page.module.scss';

export default function HomePage() {
    return (
        <main className={cls.container}>
            <h1 className={cls.container_text}>Hello, Next.js!</h1>
        </main>
    );
}
